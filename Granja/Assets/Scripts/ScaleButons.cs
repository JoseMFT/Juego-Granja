using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class ScaleButons: MonoBehaviour {

    Vector3 originalSize = gameObject.Transform.localScale ();

    public void ScaleUp () {
        LeanTween.scale (gameObject, originalSize * 1.1f, 0.25f).setEaseInCubic ();
    }

    public void ScaleDown () {
        LeanTween.scale (gameObject, originalSize, 0.25f).setEaseOutCubic ();
    }
}
    */

